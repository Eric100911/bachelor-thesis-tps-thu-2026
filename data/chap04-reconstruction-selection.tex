% \chapter{事例重建与选择策略}
\chapter{事例重建与选择}
\label{chap:reconstruction-selection}

\section{分析策略概述}
\label{sec:analysis-overview}

% [TODO] 简要说明分析策略的整体逻辑：从探测器信息出发，逐级重建物理对象，最终构建事例级候选并给出筛选后的事例样本。

基于第3章介绍的CMS探测器与数据样本，本章按照自下而上的层级顺序组织：首先介绍初级顶点与带电径迹的重建方法（第\ref{sec:pv-track-reco}节），然后说明触发条件与事例级在线选择（第\ref{sec:trigger-event-selection}节）。经过触发筛选的事例进入离线重建流程，依次描述缪子（第\ref{sec:muon-reco-selection}节）、$J/\psi$候选（第\ref{sec:jpsi-reco}节）和$\phi$候选（第\ref{sec:phi-reco}节）的重建与选择，进而构建事例级的$J/\psi J/\psi\phi$候选组合（第\ref{sec:triplet-reco}节），最后给出筛选得到的$4.8829\times10^4$个候选事例（第\ref{sec:selected-candidate-sample}节）。每一级选择均在信号效率与本底抑制之间寻求平衡，为第\ref{chap:signal-efficiency}章的三维质量谱拟合提供输入。

\section{初级顶点与径迹重建}
\label{sec:pv-track-reco}

在CMS事例重建中，初级顶点（Primary Vertex, PV）重建大致分为顶点寻找和顶点拟合两个步骤。前者将同一次束团对撞产生的带电径迹按其空间位置进行聚类，形成若干顶点候选；后者则对每个候选径迹集合估计其顶点位置及其协方差，并提供施加顶点位置约束后的相关径迹参数和协方差，以及每条径迹在顶点拟合中的权重。通过这一顶点重建过程，对撞顶点的空间位置、相对束流线的偏离以及每条径迹与各顶点的关联得以确定，为后续物理对象重建和事例选择提供了必要的空间和关联信息。

重建程序首先从带电径迹集合中，利用束斑（Beam Spot, BS）信息，筛选出适合进行初级顶点重建的带电径迹。默认的筛选条件列于表~\ref{tab:pv-reco-track-selection}；表中归一化径迹拟合 $\chi^2$ 由 Kalman 滤波得到，反映径迹重建的整体质量。这些要求排除了重建质量差、相对束流线的碰撞参数大、测量不确定度大或径迹位于径迹探测器主要几何接收范围之外的径迹，从而提高了顶点寻找过程的稳定性。

\begin{table}
  \centering
  \begin{threeparttable}[c]
    \caption{初级顶点重建中带电径迹的默认筛选条件。}
    \label{tab:pv-reco-track-selection}
    \begin{tabular}{ll}
      \toprule
      物理量 & 默认条件 \\
      \midrule
      归一化径迹拟合 $\chi^2$ & $< 10$ \\
      横向碰撞参数显著性 $|d_0|/\sigma_{d_0}$ & $< 4$ \\
      横向碰撞参数不确定度 $\sigma_{d_0}$ & $< \SI{1.0}{\centi\meter}$ \\
      纵向碰撞参数不确定度 $\sigma_{d_z}$ & $< \SI{1.0}{\centi\meter}$ \\
      横动量 $p_{\mathrm{T}}$ & $> \SI{0}{\giga\eV}$ \\
      赝快度 $\eta$ & $|\eta| < 2.4$ \\
      像素探测器击中层数 & $\ge 2$ \\
      硅径迹探测器击中层数 & $\ge 5$ \\
      径迹质量标志 & 接受任意质量标志 \\
      \bottomrule
    \end{tabular}
  \end{threeparttable}
\end{table}

经预选的径迹随后按照其在束流方向的位置进行聚类。用于聚类的 $z$ 坐标为碰撞点（impact point, IP）的 $z$ 坐标 $z_{\mathrm{IP}}$，定义为径迹到束流线最近点的位置。

径迹聚类还需要考虑 $z_{\mathrm{IP}}$ 的不确定度。这一不确定度既来自径迹重建算法给出的本征 $z$ 位置不确定度 $\sigma_{d_z}$，也来自束斑的横向宽度，后者会导致径迹碰撞点位置产生额外的 $z$ 方向不确定度 $\sigma_{z,\mathrm{BS}}$。在默认配置下，径迹聚类算法还通过加入一个本征顶点尺寸项来考虑初级顶点位置的本征尺度。这一本征顶点尺度为：
\begin{equation}
\label{eq:vtx-resolution}
  \sigma_{\mathrm{vtx}} = \SI{0.006}{\centi\meter}.
\end{equation}

由此得到有效的 $z$ 方向位置不确定度 $\sigma_{z,\mathrm{eff}}$：
\begin{equation}
\label{eq:z-resolution}
  \sigma_{z,\mathrm{eff}}^2
  =
  \sigma_{d_z}^2
  + \sigma_{z,\mathrm{BS}}^2
  + \sigma_{\mathrm{vtx}}^2
\end{equation}

在聚类过程中，赋予每条径迹的权重与 $\sigma_{z,\mathrm{eff}}^2$ 成反比。这降低了 $z$ 位置不确定度较大的径迹对聚类结果的影响，提高了聚类过程的稳定性和准确性。

径迹聚类采用沿 $z$ 方向的一维确定性退火（deterministic annealing, DA）算法~\cite{roseDeterministicAnnealingApproach1990,roseDeterministicAnnealingClustering1998}。该算法从较高的有效温度出发，允许径迹通过软权重与多个顶点原型同时关联；随后逐步降低温度，使不同 $z$ 位置的顶点候选逐渐分离并收敛~\cite{Chabanat:2005zz}。算法的关键参数（退火因子、终止温度、顶点合并距离、径迹降权尺度等）取自 CMSSW 顶点重建模块的默认配置。这些设置使算法能够在事例堆积环境中将来自不同 $pp$ 对撞的径迹分离为多个顶点候选，同时降低非初级径迹或离群径迹对聚类结果的影响。

对每个径迹团，随后采用自适应顶点拟合器（Adaptive Vertex Fitter, AVF）进行三维顶点拟合。AVF 是一种迭代加权拟合方法：在每次迭代中，算法根据每条径迹与当前顶点估计值的兼容性为径迹赋予权重。与顶点位置不兼容的径迹被逐步降权，而与顶点兼容的径迹保持高权重。默认的几何退火参数对应于 $\chi^2$ 截断尺度为 $2.5$。因此，AVF 不需要在拟合前对径迹是否属于某一顶点做出硬性判断，而是在拟合过程中通过软权重抑制离群径迹。拟合完成后，每个顶点候选必须满足有效性要求，且其与束流线在横向平面内的距离须小于 $\SI{1.0}{\centi\meter}$。

确定初级顶点后，每条带电径迹需与一个已重建顶点建立关联。关联判断中常用的量为纵向碰撞参数 $d_z$（径迹相对于参考顶点的 $z$ 坐标差）和横向碰撞参数 $d_{xy}$（径迹到顶点在横平面内的最短距离）。主要的关联判据汇总于表~\ref{tab:pv-association}。

\begin{table}
  \centering
  \begin{threeparttable}[c]
    \caption{带电粒子流候选与初级顶点关联的主要判据。}
    \label{tab:pv-association}
    \begin{tabular}{p{0.22\linewidth}p{0.68\linewidth}}
      \toprule
      类别 & 判据及物理含义 \\
      \midrule
      顶点拟合隶属关系 &
      若径迹已被用于某个初级顶点的拟合，则优先与该顶点关联。这是最强的关联方式，表明该径迹在顶点重建阶段已对顶点位置的估计做出贡献，并与该顶点具有高兼容性。
      \\
      瞬发类 $d_z$ 兼容性
      &
      若径迹未被显式用于顶点拟合，则比较其相对各顶点的纵向碰撞参数。默认情况下，距离最近的兼容顶点需满足
      $|d_z| < \SI{0.1}{\centi\meter}$，
      $|d_z|/\sigma_{d_z,\mathrm{eff}} < 5$，
      且径迹自身需满足 $\sigma_{d_z}<\SI{0.05}{\centi\meter}$。
      当这些条件满足时，认为径迹在束流方向上与顶点兼容，且符合瞬发带电粒子的拓扑特征。
      \\
      重味强子衰变径迹
      &
      若径迹不满足瞬发类判据，算法进一步考虑其来自长寿命粒子（如 $B_s$ 强子）次级衰变的可能性。此时将搜索邻近喷注，默认要求为
      喷注 $p_{\mathrm T}>\SI{25}{\giga\eV}$ 且
      径迹与喷注满足 $\Delta R<0.5$。
      随后要求径迹相对候选顶点满足
      $|d_z|<\SI{0.1}{\centi\meter}$ 且 $|d_{xy}|<\SI{0.1}{\centi\meter}$，
      并且径迹到喷注轴的距离须小于 $\SI{0.07}{\centi\meter}$。若存在满足这些判据的顶点，则将该径迹与该顶点关联，表明虽然该径迹不是瞬发的，但其与来自该顶点的喷注内部的位移衰变拓扑兼容。
      \\
      未重建的初级顶点情形
      &
      若径迹无法与任何已重建顶点强关联，但其相对第一个顶点的横向碰撞参数较小，即 $|d_{xy}|<\SI{0.01}{\centi\meter}$ 且 $|d_{xy}|/\sigma_{d_{xy}}<2$，则认为该径迹可能源自一个未被成功重建的初级顶点，并将其分配给 $z$ 方向上最近的已重建顶点。
      \\
      与最近 $z$ 顶点的弱关联
      &
      若以上判据均不满足，但径迹 $d_z$ 仍可相对于每个已重建顶点计算，则将该径迹弱关联到 $d_z$ 显著性最小的顶点。此步骤仅作为后备方案，表明在可用的已重建顶点中，该顶点在 $z$ 方向上距离该径迹最近。
      \\
      \bottomrule
    \end{tabular}
  \end{threeparttable}
\end{table}

初级顶点重建完成后，CMS粒子流（particle-flow, PF）算法重建的候选将与已重建的初级顶点进行关联。关联策略采用表~\ref{tab:pv-association} 所列的层级判据，依物理可靠性从强到弱依次为：顶点拟合隶属关系、瞬发类 $d_z$ 兼容性、重味强子衰变径迹匹配，以及最近 $z$ 距离弱关联。

CMS硅径迹探测器中带电粒子的径迹采用基于 Kalman 滤波方法的迭代组合径迹寻找器（Combinatorial Track Finder, CTF）进行重建~\cite{Fruhwirth:1987:Kalman,CMS_TDR_CH6_TRACKING}。重建序列依次包括击中聚类、种子生成、模式识别（径迹寻找）、歧义消除（移除共享过多击中的冗余径迹）和最终径迹拟合。

原始信号首先在像素和条状传感器中进行聚类。在像素探测器中，相邻像素间的电荷共享提供二维位置信息；在硅微条探测器中，相邻条上超过阈值的信号被合并为一维击中团。聚类完成后，径迹重建从种子生成开始，以四个探测器击中点为一个种子，并由此估计初始螺旋线参数及其协方差矩阵。

CTF 将每个种子向外逐层传播穿过后续径迹探测器层，利用 Kalman 滤波寻找与径迹候选兼容的击中，并在每一层更新径迹参数。当一个候选可以通过多种击中分配方式传播时，算法并行维护多个径迹候选，并在每一层根据与分配击中的 $\chi^2$ 兼容性进行剪枝。遍历所有径迹探测器层后，从外向内应用第二次 Kalman 滤波，以获得每个测量面上的最优参数估计。

为提高不同径迹拓扑的重建效率，CTF以多次迭代方式运行。每次迭代后，明确分配给高质量径迹的击中从后续搜索中移除，以减少组合复杂度。若数条重建径迹共享较多击中，歧义消除和径迹集合合并模块依据共享击中比例、有效击中数、丢失击中数和拟合质量选择保留候选。最终径迹由每轮的质量分类器赋予
\texttt{loose}、\texttt{tight}和\texttt{highPurity}等质量标志；本分析对构成$\phi$候选的带电径迹明确要求\texttt{highPurity}。

\section{触发与事例级选择}
\label{sec:trigger-event-selection}

本分析使用的数据由 CMS 在线触发系统筛选。效率计算中的事例级触发步骤采用
\texttt{HLT\_Dimuon0\_Jpsi3p5\_Muon2\_v}与
\texttt{HLT\_DoubleMu4\_3\_LowMass\_v}两条路径的逻辑或，并进一步检查相应候选缪子与触发对象和过滤器的匹配；主要触发条件已在第\ref{chap:experiment-dataset}章表~\ref{tab:trigger-conditions}中列出。
这两条路径均针对低质量双缪子末态，有利于保留包含$J/\psi\to\mu^+\mu^-$衰变的事例。

\section{缪子重建与选择}
\label{sec:muon-reco-selection}

CMS 中的缪子候选通过联合硅径迹探测器和缪子系统的信息进行重建；缪子探测器的硬件结构和几何覆盖已在第~\ref{chap:experiment-dataset}~章介绍。离线重建中，standalone 缪子径迹首先由缪子系统中的局部径迹段构建，并在考虑能量损失、多次散射和磁场非均匀性的条件下向内外传播。global 缪子重建则将 standalone 缪子径迹与硅径迹探测器中的 tracker 径迹进行匹配，并利用两个探测系统的击中进行联合拟合。由于综合利用了内外两个探测系统的信息，global 缪子重建在横动量分辨率上通常优于单独使用缪子系统的重建~\cite{CMS_TDR_CH9_MUONS,CMS_MUON_PERF_13TEV}。

在线触发和离线重建都依赖缪子系统与硅径迹探测器之间的一致性匹配。对本分析而言，低横动量缪子的重建效率和双缪子质量分辨率直接影响 $J/\psi$ 候选的接受度；因此后续选择采用针对低动量缪子优化的鉴别工作点，并通过触发效率修正吸收在线筛选带来的事例级效率影响。

本分析采用 CMS soft muon 工作点作为缪子鉴别判据~\cite{CMS_MUON_PERF_13TEV}。该工作点针对低横动量缪子进行了优化，特别适用于 $B$ 物理和夸克偶素相关的末态。在探测器层面，soft muon 工作点由以下要求所定义：

\begin{enumerate}
  \item 候选必须被重建为 tracker 缪子，且其内层 tracker 径迹需满足 high-purity 质量要求。
  \item tracker 径迹必须在至少六层硅径迹探测器层中具有击中，其中至少包含一个像素探测器击中；
  \item 候选必须通过\texttt{TMOneStationTight}径迹段匹配：至少一个缪子站中的匹配径迹段须满足局部位置残差或其pull的严格匹配条件；实现中的位置残差阈值为\SI{3}{\centi\meter}，pull阈值为3。
  \item 候选必须与初级顶点兼容，横向和纵向碰撞参数需分别满足 $|d_{xy}| < \SI{0.3}{\centi\meter}$ 和 $|d_z| < \SI{20}{\centi\meter}$。
\end{enumerate}

soft muon 鉴别策略的核心思想是主要依赖内层硅径迹探测器提供的高精度动量和碰撞参数测量，同时用缪子系统中的径迹段匹配来确认缪子假设。这在低横动量区域实现了高缪子鉴别效率。

在 soft muon 鉴别的基础上，本分析对缪子施加如下运动学选择条件：

\begin{itemize}
  \item 赝快度：$|\eta| < 2.4$，确保缪子位于 CMS 硅径迹探测器和缪子谱仪的有效几何接收范围内。
  \item 横动量：根据缪子探测器触发接受度和重建分辨率随 $\eta$ 的变化，采用分区阈值：
    \begin{itemize}
      \item 桶部区域（$|\eta| < 1.2$）：$p_{\mathrm{T}} > \SI{3.5}{\giga\eV}$；
      \item 端盖区域（$1.2 \leq |\eta| < 2.4$）：$p_{\mathrm{T}} > \SI{2.5}{\giga\eV}$。
    \end{itemize}
\end{itemize}

\section{\texorpdfstring{$J/\psi$}{J/psi} 候选重建}
\label{sec:jpsi-reco}


$J/\psi$ 介子通过其双缪子衰变道 $J/\psi \to \mu^+\mu^-$ 进行重建。该衰变道的世界平均分支比和不确定度已在第~\ref{sec:theory-quarkonium-production}~节给出。尽管双缪子分支比较小，但末态中的两个缪子提供了清晰的实验特征。借助 CMS 优异的缪子重建和鉴别能力，此衰变道可提供高信噪比，并便于在复杂本底环境中进行信号提取。

$J/\psi$ 候选由满足第~\ref{sec:muon-reco-selection}~节所述选择条件的缪子中选取带相反电荷的缪子对进行重建。每个候选需满足以下判据：

\begin{enumerate}
  \item 不变质量窗口：双缪子不变质量 $m(\mu^+\mu^-)$ 需满足 $\SI{2.9}{\giga\eV}\leq m(\mu^+\mu^-)\leq \SI{3.3}{\giga\eV}$；
  \item 横动量和快度：$J/\psi$ 候选的横动量需满足 $p_{\mathrm T}^{J/\psi} > \SI{6.0}{\giga\eV}$，快度需满足 $|y^{J/\psi}| < 2.5$；
  \item 顶点拟合质量：两条缪子径迹必须拟合到公共衰变顶点，且顶点拟合概率须大于 $1\%$。
\end{enumerate}

在上述选择条件中，不变质量窗口兼顾了 $J/\psi$ 的质量峰分辨率与连续本底的抑制。横动量和快度选择基于 CMS 的触发效率和重建性能。顶点拟合质量要求有助于抑制组合本底，并确保两条缪子在空间上与源自同一衰变顶点一致。

\section{\texorpdfstring{$\phi$}{phi} 候选重建}
\label{sec:phi-reco}

$\phi$ 介子通过其主要衰变道 $\phi \to K^+K^-$ 进行重建，其世界平均分支比和不确定度见第~\ref{sec:theory-quarkonium-production}~节。在 CMS 实验中，对于横动量在 \si{\giga\eV} 量级的带电强子，区分 $K^\pm$ 与 $\pi^\pm$ 的能力有限。因此，本分析采用 CMS 多夸克偶素分析中常用的策略：所有通过基本质量选择且未被鉴别为缪子的带电径迹均赋予 $K^\pm$ 质量假设~\cite{CMS_TRI_JPSI}。

$\phi$ 候选由满足以下条件的带电径迹中选取带相反电荷的径迹对进行重建：

\begin{enumerate}
  \item 径迹质量及初级顶点关联：Ntuple生产阶段要求带电径迹满足 CMS high-purity 质量要求、归一化径迹拟合$\chi^2<8$、有效击中数大于4，且\texttt{pat::PackedCandidate::fromPV()}至少达到\texttt{PVLoose}级别。
  \item 运动学接收：径迹赝快度需满足 $|\eta| < 2.4$，横动量需满足 $p_{\mathrm T} > \SI{2.0}{\giga\eV}$。
  \item 不变质量窗口：在 $K^\pm$ 质量假设下，双径迹不变质量 $m(K^+K^-)$ 需满足 $\SI{0.99}{\giga\eV}\leq m(K^+K^-)\leq \SI{1.07}{\giga\eV}$，对应于覆盖 PDG 给出的 $\phi$ 质量 $\SI{1.0195}{\giga\eV}$ 的宽质量窗口~\cite{PDG2024}。
  \item 横动量和赝快度：$\phi$ 候选需满足 $p_{\mathrm T}^{\phi} > \SI{4.0}{\giga\eV}$及$|y^\phi|<2.5$。
  \item 顶点拟合质量：两条带电径迹必须能够拟合到公共顶点，且顶点拟合概率须大于 $1\%$。
\end{enumerate}

上述选择策略的核心思路是通过$p_{\mathrm T}$阈值筛选硬散射产生的$\phi$介子，同时以径迹质量、不变质量窗口和顶点拟合要求抑制组合本底。与$J/\psi$候选的选择（第\ref{sec:jpsi-reco}节）相比，$\phi$候选的挑选更侧重于压低来自软QCD过程的贡献。以下第\ref{sec:triplet-reco}节将$J/\psi$和$\phi$候选组合为事例级的$J/\psi J/\psi\phi$候选。

\section{\texorpdfstring{$J/\psi J/\psi\phi$}{J/psi J/psi phi} 候选重建}
\label{sec:triplet-reco}

在重建并筛选出单个末态粒子，即缪子、$J/\psi$ 候选和 $\phi$ 候选之后，本分析构建事例级的 $J/\psi + J/\psi + \phi$ 候选组合：

\begin{enumerate}
  \item 从事例中选取两个满足第~\ref{sec:jpsi-reco}~节所述条件的 $J/\psi$ 候选，要求它们不共享任何缪子径迹；
  \item 从事例中选取一个满足第~\ref{sec:phi-reco}~节所述条件的 $\phi$ 候选，要求其两条带电径迹不与两个 $J/\psi$ 候选使用的缪子径迹重叠；
  \item 将两个$J/\psi$候选的四条缪子径迹进行共同顶点拟合，并要求对应的\texttt{DiOnia}顶点拟合有效且通过设定的顶点质量要求；
  \item 对构成两个 $J/\psi$ 候选的四条缪子径迹和构成 $\phi$ 候选的两条带电径迹，采用第~\ref{sec:pv-track-reco}~节介绍的带电径迹--初级顶点关联方法，要求六条径迹均关联到同一个初级顶点，即通过数据生产中标记为 \texttt{Pri\_assocPVPass} 的事例级选择。该要求用于保证三个介子候选与同一次重建的 $pp$ 对撞相联系。
\end{enumerate}

对于每个保留的候选，两个$J/\psi$候选始终按横动量排序：
\begin{equation}
  p_{\mathrm{T}}(J/\psi_1) > p_{\mathrm{T}}(J/\psi_2),
\end{equation}
其中$J/\psi_1$和$J/\psi_2$分别表示较高横动量（leading）和较低横动量（subleading）的$J/\psi$候选。后文所有带下标1和2的$J/\psi$量均遵循此约定。

若一个事例中有多个候选组合通过当前数据合并脚本中的质量窗、运动学和soft-muon要求，则选取$J/\psi+J/\psi+\phi$之横动量平方和最大的组合。
这一选择是出于排除组合本底的考虑：在随机组合的背景下，较低的横动量一般与软QCD过程或其他本底的贡献相关，而信号事件更可能具有较高的横动量。通过选择横动量平方和最大的组合，可以在保持信号效率的同时有效抑制来自随机组合的本底。

\section{筛选候选样本}
\label{sec:selected-candidate-sample}

将上述选择应用于 Run~3 数据样本，并在每个事例中按照保留一个最佳候选后，共得到 $4.8829\times10^4$ 个 $J/\psi J/\psi \phi$ 候选事例。这些事例构成第~\ref{chap:signal-efficiency}章所述信号提取的输入样本。所选候选在拟合前的不变质量分布见图~\ref{fig:prefit-mass-distributions}，$J/\psi$ 和 $\phi$ 的共振峰在组合本底之上清晰可见。三维拟合在质量谱上的投影及其他更细致的拟合结果等将在下一章介绍。

\begin{figure}[htbp]
  \centering
  \subcaptionbox{$m(\mu^+\mu^-)_1$}{\includegraphics[width=0.32\textwidth]{figures/chap04-reconstruction-selection/raw-mass-plot/sel_Jpsi_1_mass.pdf}}
  \subcaptionbox{$m(\mu^+\mu^-)_2$}{\includegraphics[width=0.32\textwidth]{figures/chap04-reconstruction-selection/raw-mass-plot/sel_Jpsi_2_mass.pdf}}
  \subcaptionbox{$m(K^+K^-)$}{\includegraphics[width=0.32\textwidth]{figures/chap04-reconstruction-selection/raw-mass-plot/sel_Phi_mass.pdf}}
  \caption{筛选后的 $J/\psi J/\psi \phi$ 候选在信号提取前的不变质量分布：$m(\mu^+\mu^-)_1$（左）、$m(\mu^+\mu^-)_2$（中）、$m(K^+K^-)$（右）。}
  \label{fig:prefit-mass-distributions}
\end{figure}
